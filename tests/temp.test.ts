import { convertMinutesToMs } from "../src/utility";

describe("Temporary, please remove", () => {
  it("should import convertMinutesToMs", () => {
    expect(convertMinutesToMs(1)).toBe(60000);
  });
});
